# Repo conventions

- **Commit straight to `main`.** This repo does not use feature branches; do
  not create `claude/*` branches or open PRs unless explicitly asked. Push
  directly to `main`. Note that pushes to `main` deploy the live github.io
  site.
